#include <stdio.h>


/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char quote[52] = "\"Programming is like building a multilingual puzzle";
puts(quote);
return (0);
}
